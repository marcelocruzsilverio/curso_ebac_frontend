const multiply = (factor1: number, factor2: number): number => factor1 * factor2;

const result = multiply(2, 2)
console.log(result);



const sayHello = (name: string): string => `Olá ${name}!`;

const welcome = sayHello('Marcelo');
console.log(welcome);
